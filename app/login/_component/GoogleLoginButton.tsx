'use client';
import React from 'react';
import styled from '@emotion/styled';

export default function GoogleLoginButton() {
  return <GoogleLoginButtonStyle>GoogleLoginButton</GoogleLoginButtonStyle>;
}

const GoogleLoginButtonStyle = styled.div`
  background-color: red;
`;
